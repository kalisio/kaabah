# Changelog

{{#each releases}}
   {{#if href}}
    ###{{#unless major}}#{{/unless}} [{{title}}]({{href}})
  {{else}}
    #### {{title}}
  {{/if}}

  {{#if tag}}
    > {{niceDate}}
  {{/if}}

  {{#each fixes}}
    - {{{commit.subject}}}{{#each fixes}}{{#if href}} [`#{{id}}`]({{href}}){{/if}}{{/each}}
  {{/each}}

{{/each}}